export type UserLoginType = {
    usernameOrEmail: string;
    password: string;
};

export type UserRegisterType = {
    username: string;
    name: string;
    email: string;
    phoneNumber: string;
    confirmPassword: string;
    password: string;
};
